#Define una función que permita conocer si el primer y último elemento de una lista son el mismo elemento. La función debe tener por nombre “iguales”. La función debe recibir como argumento una lista de números enteros con longitud mayor a 2. La función debe retornar verdadero o falso si el primer y último elemento son el mismo número.
def iguales(lista):
    if len(lista) <= 2:
        return None
    return lista[0] == lista[-1]
